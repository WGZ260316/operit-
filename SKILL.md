---
name: 插件工具创造决策
description: 当用户提交插件/工具/技能/命令创造申请时，先把"创造"当作最后手段：强制先解析需求为能力画像，再到多源市场（当前可用包、插件市场、GitHub/npm/pip/MCP 等）检索同功能现成方案，按覆盖度四级裁决——能复用则激活、能扩展则配置、能组合则编排，全部走不通才进入创造流程且 MVP 先行。触发词：帮我做个插件/工具/技能/脚本、写个XX实现Y、有现成的吗、别造轮子。
---

# 插件工具创造决策技能（Plugin Request → Decide → Reuse-or-Build）

> 用途：当用户提交**插件/工具/技能创造申请**时，先把"创造"当作**最后手段**，
> 严格按"先搜市场 → 比对适配 → 能复用绝不重造 → 确实无解才创造"的顺序决策。

---

## 一、触发场景（满足其一即触发本技能）

- 用户要求"帮我做一个插件 / 工具 / 技能 / 命令 / 自动化"；
- 用户描述某需求后隐含"造轮子"倾向（如"有没有办法实现 X""给我写个脚本搞定 Y"）；
- 用户提交了插件创造申请单、功能描述、API 对接、界面操作自动化等诉求。

---

## 二、决策总原则（可执行优先级）

1. **可复用 > 可扩展 > 可组合 > 才创造**
   - 优先级 A：市场已有工具直接满足 → **推荐/激活/引入**，不创造；
   - 优先级 B：已有工具 80% 覆盖 → **推荐扩展配置/二次封装/组合多个**，不重造；
   - 优先级 C：市场找不到、且手头能力明显不足 → 才进入**创造流程**。
2. **先验证、后承诺**：任何"已有方案"都必须给出名称、来源、覆盖点，而不是空口说"应该可以"。
3. **拒绝盲目创造**：凡是市场已有成熟方案却仍坚持重写的，必须给出正当理由（授权、离线、定制深度、性能等）。
4. **决策留痕**：最终结论（复用 / 扩展 / 创造）都要能回答"为什么不做另一种选择"。

---

## 三、标准决策流程

### Step 1 · 需求澄清与拆解（不跳过）
先不要急着搜或造。把用户一句话需求拆成结构化要素：

- **核心能力**（要完成什么：取数？转换？UI 操作？对话？画图？……）
- **输入 / 输出**（数据格式、文件类型、触发方式、返回要求）
- **约束**（平台：Android/桌面/云端；权限：root/Shizuku；离线 or 联网；性能；安全；授权）
- **验收标准**（怎样算"满足需求"——可用打分项衡量）
- **优先级**（必须项 / 想当然项，区分硬需求与偏好）

产出：一段"需求画像"，作为后续检索关键词与比对基准。

### Step 2 · 市场检索（必须执行，先于一切创造）
按需求画像提炼 3–6 个**检索关键词**（能力词 + 平台词 + 格式词），多源并行检索：

1. **当前会话可用包**：本系统已列出的包列表（`use_package` 可见的包），看是否已有功能重叠；
2. **插件 / 技能市场**：Operit 插件市场、技能库、工作流模板库等平台内市场；
3. **代码与包仓库**：GitHub、Gitee、npm、pip、Maven、Docker Hub、MCP 市场、Awesome 清单等；
4. **社区/厂商**：官方插件商店、云市场（阿里/腾讯）、开源社区、RPA/自动化社区；
5. **关键词变体**：同义中英文、动词+名词组合、近义能力词（如"表格转PDF"也搜"导出""转换""渲染"）。

> 检索不是"点开一两个链接"，而是要为每个候选记录：名称、来源链接、核心功能、作者/维护状态、
> 授权、覆盖度。**找不到 ≠ 不存在**，至少换 3 组关键词、2 个平台再下结论。

### Step 3 · 候选比对与裁决
将检索到的候选与 Step 1 的验收标准逐条比对，判定层级：

| 层级 | 判定 | 行动 |
|------|------|------|
| 完全覆盖 | 核心能力+约束+验收全部满足 | **直接复用**：推荐引入/激活该插件，说明配置与用法 |
| 高覆盖 | 80%+ 满足，仅需少量配置/参数 | **扩展复用**：指导配置、二次封装或参数化调用，不重写 |
| 部分覆盖 | 覆盖核心但缺 1–2 项 | 优先**组合**（多工具协作）或**请求扩展**；仍不足才考虑轻量创造外壳 |
| 无匹配 | 无任何可用方案，或全部方案有硬伤（授权/维护/离线/安全） | 进入 **Step 4 创造流程** |

裁决输出：**结论 + 命中/未命中的候选清单 + 选定理由**，必要时向用户汇报并让其确认"是复用还是要自研"。

### Step 4 · 创造流程（仅当裁决为"无匹配"时进入）
1. **写方案**：目标、输入输出、架构/流程图、用到的现有能力（避免重复调用）、边界；
2. **最小可行优先**：先做一个覆盖核心验收的 MVP，能跑通再扩展，不要一步到位堆功能；
3. **命名与归档**：遵守平台包/技能命名规范，放到正确目录，写清用途与依赖；
4. **测试与联调**：用真实输入跑通，核对输出符合验收标准；有平台依赖（root/Shizuku/网络）要实测；
5. **交付说明**：向用户说明能力、用法、限制、以及"为何市场方案不可用"的结论。

---

## 四、市场检索的工具与提示

- 优先用**多平台搜索**（网页/代码搜索/包搜索）而非单一引擎；
- 检索时给 AI 明确约束："查找能完成 X 的现成插件/库/包，重点给出可引入方式与维护状态"；
- 若检索工具可用，把候选**链接或包名**一并取回做二次核对，避免只凭标题判断；
- 对可疑/不活跃/权限过大的包，标记风险并建议绕行。

---

## 五、必须向用户交代的三件事

1. **决策过程可回溯**：搜了哪些地方、比对过哪些候选（哪怕最后没有）；
2. **复用结论要可落地**：具体用哪个、怎么激活/配置，而不是"可以找找看"；
3. **创造结论要合理**：为什么市场没有满足——附上被否候选的硬伤证据。

---

## 六、守则清单（自检）

- [ ] 我是否真的先检索了市场，而不是直接动手写？
- [ ] 我是否区分了"硬需求"与"偏好"，避免为偏好过度创造？
- [ ] 已有方案是否被我用"覆盖度+维护+授权+平台"四维核实过？
- [ ] 结论（复用/扩展/创造）是否给出理由并让用户知情？
- [ ] 若创造：是否 MVP 优先、遵守平台规范、真实测试过？
- [ ] 我是否避免了两类失败：①有现成却硬造；②无现成却反复推诿不造。

---

## 七、一句话核心

> **需求先落地为"能力画像"，市场检索三组关键词两平台起步；能复用则激活、能扩展则配置、
> 能组合则编排，全部走不通——才轮到你动手创造，且必须 MVP 先行、留痕说明。**
